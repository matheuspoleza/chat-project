import styled from 'styled-components';

export const Container = styled.div`
  max-width: 920px;
  margin-left: auto;
  margin-right: auto;
  margin-top: 100px;
`;

export const Input = styled.div`
  margin-right: 8px;
`;

const DashboardStyles = { Container, Input };

export default DashboardStyles;
